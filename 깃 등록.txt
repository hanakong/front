git bash에서

git 등록하기
1)유저 등록하기
$ git config --global user.name "hanakong"

2)유저의 이메일 주소 등록하기
$ git config --global user.email "wonbin1301@gmail.com"

3) 등록 확인
$ git config --list


vsc에서 terminal -> new terminal
<명령어>
git init : 파일 초기화

git add .

-- git status : 현재 상태보기

git commit -m "first commit" : pc의 git 영역으로 감

이제 깃허브랑 연결해야함

git remote add origin https://github.com/hanakong/front.git

-- git remote -v : 현재 잘 연결되어있는지 확인

git push origin master 입력후 token 넣기

github 세팅에 

git push -u origin main :

다음 등록시
git add . 
git commit -m "날짜"
git push origin ~~
만 진행하면 됨

가져올 때는 
git pull origin master

만약에 pull받은 폴더의 파일을 손상시켰을 경우에는 다시 fetch시킨 후 받아준다.
git fetch --all
이후
git reset --hard origin/master